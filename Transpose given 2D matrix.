Question:A program to perform transpose given 2D matrix.
Code:
#include <iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int m[n][n],o[n][n];
    for (int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            cin>>m[i][j];
        }
    }
    cout<<"Transpose matrix is :"<<endl;
    for (int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            o[i][j]=m[j][i];
            
            cout<<o[i][j]<<" ";
        }
        
        cout<<endl;
    }
}
